Analyze if '{{ term }}' is a valid {{ type }} and extract its meaning.

Contexts where the term appears:
{% for ctx in occurrences_contexts[:15] %}
{{ loop.index }}. {{ ctx }}

{% endfor %}

{% if cooccurring_terms %}
Co-occurring terms:
{% for cooc_term in cooccurring_terms.keys() | list | slice(10) %}
- {{ cooc_term }}
{% endfor %}
{% endif %}

Guidelines:
- Always extract the term exactly as it appears.
- For acronyms, look for parentheses or definitions nearby.
- For keywords, use the word itself as the full form.
- The meaning should be concise and context-aware.
- Use "unknown" only if you cannot confidently classify the term.
